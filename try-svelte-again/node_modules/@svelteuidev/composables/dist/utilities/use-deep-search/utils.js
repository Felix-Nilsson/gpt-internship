export function getKeysFromObject(object) {
    return Object.keys(object);
}
export function searchForValues(obj) {
    // TODO
}
export function searchForKeys(obj) {
    const nestedKeys = [];
    const keys = getKeysFromObject(obj);
    for (const key of keys) {
        const value = obj[key];
        if (typeof value === 'object') {
            // if we find a value is an object we must recursively search through it to find all the keys.
        }
    }
    return [...keys, ...nestedKeys];
}
