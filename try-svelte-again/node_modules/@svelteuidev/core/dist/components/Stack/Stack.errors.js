/** Error codes for component
 *
 * `Object.freeze` is needed to keep modification outside of the object unavailable
 *
 * ## Code 1:
 * No Errors right now
 *
 */
export const StackErrors = Object.freeze([
    {
        error: true,
        message: 'Message',
        solution: `
                If your component looks like this:
                
                &lt;Stack&gt; Button Text &lt;/Stack&gt;
                        ^^^^^^^^ - Try removing this
                `
    }
]);
