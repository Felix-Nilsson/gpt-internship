<script>import { Box } from '../Box';
import useStyles from './TypographyProvider.styles';
export let use = [], element = undefined, className = '', override = {}, primaryColor = 'blue', underline = true;
export { className as class };
$: ({ cx, classes, getStyles } = useStyles({ primaryColor, underline }, { name: 'TypographyProvider' }));
</script>

<Box
	bind:element
	class={cx(className, classes.root, getStyles({ css: override }))}
	{use}
	{...$$restProps}
>
	<slot />
</Box>
