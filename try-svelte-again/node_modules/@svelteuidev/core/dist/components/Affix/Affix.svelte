<script>import { Box } from '../Box';
import { Portal } from '../Portal';
export let use = [], element = undefined, className = '', override = {}, target = 'body', position = { bottom: 0, right: 0 }, zIndex = 200;
export { className as class };
</script>

<Portal {zIndex} {target}>
	<Box
		bind:element
		class={className}
		css={{ position: 'fixed', ...position, ...override }}
		{use}
		{...$$restProps}
	>
		<slot />
	</Box>
</Portal>
