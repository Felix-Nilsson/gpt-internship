import type { SvelteUISize } from '../../../../styles';
type Breakpoints<T> = Partial<Record<SvelteUISize | (string & Record<never, never>), T>>;
export interface AppShellTheme {
    spacing: Record<SvelteUISize, number>;
    breakpoints: Record<SvelteUISize, number>;
}
export declare const theme: AppShellTheme;
export declare function getSortedBreakpoints<T>(breakpoints: Breakpoints<T>, theme: AppShellTheme): [number, T][];
export {};
