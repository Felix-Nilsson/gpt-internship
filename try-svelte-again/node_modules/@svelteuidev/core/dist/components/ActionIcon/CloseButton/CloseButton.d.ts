import type { SvelteUINumberSize } from '../../../styles';
import type { ActionIconProps } from '../ActionIcon';

export interface CloseButtonProps extends ActionIconProps {
	iconSize?: SvelteUINumberSize;
}
