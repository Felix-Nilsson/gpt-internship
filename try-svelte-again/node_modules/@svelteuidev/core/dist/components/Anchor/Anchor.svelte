<script>import useStyles from './Anchor.styles';
import Text from '../Text/Text.svelte';
export let use = [], element = undefined, className = '', override = {}, root = 'a', align = 'left', color = 'blue', transform = 'none', weight = 'normal', gradient = { from: 'indigo', to: 'cyan', deg: 45 }, inline = true, lineClamp = undefined, underline = true, inherit = false, href = '', tracking = 'normal', external = false;
export { className as class };
$: ({ cx, classes, getStyles } = useStyles(null, { name: 'Anchor' }));
</script>

<!--
@component
**UNSTABLE:** new API, yet to be vetted.

Display an anchor text that is a wrapper around `Text` component using an `a` as the default
root.

@see https://svelteui.org/core/anchor
@example
    ```svelte
    <Anchor href="https://svelteui.org/">Main Page</Anchor>
    <Anchor root={ Button } href="https://svelteui.org/" target="_blank">Documentation</Anchor>
    <Anchor root={ Link } to="/home" color='violet' size='lg'>Click here</Anchor>
    ```
-->
<Text
	bind:element
	class={cx(className, classes.root, getStyles({ css: override }))}
	variant="link"
	{use}
	{root}
	{align}
	{color}
	{transform}
	{weight}
	{gradient}
	{inline}
	{lineClamp}
	{underline}
	{inherit}
	{href}
	{tracking}
	target={external ? '_blank' : null}
	rel={external ? 'noreferrer noopener' : null}
	{...$$restProps}
>
	<slot>Enter some anchor text</slot>
</Text>
