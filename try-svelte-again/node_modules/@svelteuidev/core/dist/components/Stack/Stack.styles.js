import { createStyles } from '../../styles';
export default createStyles((theme, { align, justify, spacing }) => {
    return {
        root: {
            display: 'flex',
            flexDirection: 'column',
            alignItems: `${align}`,
            justifyContent: `${justify}`,
            gap: theme.fn.size({ size: spacing, sizes: theme.space })
        }
    };
});
