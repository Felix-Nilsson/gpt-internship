import { createStyles } from '../../styles';
export default createStyles((theme, { align, spacing, justify }) => {
    return {
        root: {
            display: 'flex',
            flexWrap: 'wrap',
            justifyContent: justify,
            alignItems: align,
            margin: (-1 * theme.fn.size({ size: spacing, sizes: theme.space })) / 2
        }
    };
});
