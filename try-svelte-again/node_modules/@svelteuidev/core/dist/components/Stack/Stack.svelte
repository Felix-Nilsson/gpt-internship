<script>import Box from '../Box/Box.svelte';
import useStyles from './Stack.styles';
export let use = [], element = undefined, className = '', override = {}, spacing = 'md', align = 'stretch', justify = 'center';
export { className as class };
$: ({ cx, classes, getStyles } = useStyles({ align, justify, spacing }, { name: 'Stack' }));
</script>

<!--
@component

Compose elements and components in a vertical flex container.

@see https://svelteui.org/core/stack
@example
    ```svelte
    <Stack align="flex-end" justify="space-between" override={{ height: 300 }}>
		<Button variant="outline">1</Button>
		<Button variant="outline">2</Button>
		<Button variant="outline">3</Button>
	</Stack>
    ```
-->

<Box
	bind:element
	{use}
	class={cx(className, classes.root, getStyles({ css: override }))}
	{...$$restProps}
>
	<slot />
</Box>
