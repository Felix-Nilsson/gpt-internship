import { createStyles } from '../../styles';
export default createStyles((_, { align, bulletSize, lineWidth }) => {
    return {
        root: {
            paddingLeft: align === 'left' ? bulletSize / 2 + lineWidth / 2 : 0,
            paddingRight: align === 'left' ? 0 : bulletSize / 2 + lineWidth / 2
        }
    };
});
