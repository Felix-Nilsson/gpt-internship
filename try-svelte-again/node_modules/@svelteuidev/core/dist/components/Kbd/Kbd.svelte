<script>import useStyles from './Kbd.styles';
import { Box } from '../Box';
export let use = [], element = undefined, className = '', override = {};
export { className as class };
$: ({ cx, classes, getStyles } = useStyles(null, { name: 'Kbd' }));
</script>

<Box
	bind:element
	root="kbd"
	class={cx(className, classes.root, getStyles({ css: override }))}
	{use}
	{...$$restProps}
>
	<slot />
</Box>
